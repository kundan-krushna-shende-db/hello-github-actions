name: "Hello Actions"
description: "Greet someone"
author: "kundanshende@github.com"

inputs:
  MY_NAME:
    description: "Who to greet"
    required: true
    default: "Kundan"

runs:
  using: "docker"
  image: "Dockerfile"

branding:
  icon: "mic"
  color: "purple"
